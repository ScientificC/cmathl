#include <stdlib.h>
#include <cml.h>

/**
 * root(f) function developed by using the exponentiation process
 *
 * @param real x
 *
 * @return real root(f)
 */

real_t
real_sqrt(real_t x)
{
        return real_root(x, (2.0));
}
