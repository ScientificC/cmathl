#ifndef CML_H
#error "Never use <easings/include/sine.h> directly; include <cml.h> instead."
#endif

#ifndef CML_EASINGS_SINE_H
#define CML_EASINGS_SINE_H

real sine_ease_in(real p);
real sine_ease_out(real p);
real sine_ease_in_out(real p);

#endif
